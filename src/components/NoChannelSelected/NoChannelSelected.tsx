import classes from "./NoChannelSelected.module.css";

const NoChannelSelected = () => {
    return (
        <div className={classes.noChannelSelected}>
            <h2>No Channel Selected</h2>
        </div>
    );
};

export default NoChannelSelected;
