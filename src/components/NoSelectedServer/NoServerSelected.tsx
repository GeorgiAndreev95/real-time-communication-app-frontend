import classes from "./NoServerSelected.module.css";

const NoServerSelected = () => {
    return (
        <div className={classes.noServerSelected}>
            <h2>No Server Selected</h2>
        </div>
    );
};

export default NoServerSelected;
